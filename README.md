# Terminal Portfolio

A retro terminal-style portfolio built with React, TypeScript, and Vite.

## 🛠️ Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/yourusername/terminal-portfolio.git
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Set up environment variables:
   ```bash
   cp .env.example .env
   ```
4. Start the development server:
   ```bash
   npm run dev
   ```

## 📦 Large 3D Models

If your 3D model is too large for GitHub (over 100MB), **do not commit it to the repository**.

See [HOSTING_LARGE_MODELS.md](./HOSTING_LARGE_MODELS.md) for instructions on how to:
1. Host the model using GitHub Releases.
2. Configure the deployment to load it dynamically.

## 🏗️ Build

To build the project for production:

```bash
npm run build
```

## 🧪 Testing

Run tests with:

```bash
npm test
```

## 📝 License

MIT
